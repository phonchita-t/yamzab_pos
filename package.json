{
  "name": "yam-zabb-pos",
  "version": "1.0.0",
  "private": true,
  "description": "Yam Zabb POS - Point of Sale for a spicy salad (som tam / yam) restaurant. Runs entirely client-side (React + localStorage) on a single device, no backend or database required.",
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev": "npm run dev --workspace client",
    "build": "npm run build --workspace client",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  },
  "engines": {
    "node": ">=20"
  }
}
